name: Deno
# Workflow disabled — not needed for static HTML/CSS/JS website
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Workflow disabled"
