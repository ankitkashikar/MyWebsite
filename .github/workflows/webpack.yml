name: Deploy Static Website

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout your repository
      - uses: actions/checkout@v4

      # Step 2: Deploy static files
      # Replace this with your hosting/deploy command if needed
      - name: Deploy static files
        run: echo "Static site deployment (replace with your deploy command)"
